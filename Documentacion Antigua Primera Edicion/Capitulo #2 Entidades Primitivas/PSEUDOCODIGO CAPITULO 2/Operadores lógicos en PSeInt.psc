Proceso principal
	Definir a,b,c Como Entero; // Se pueden definir varias variables en una sola linea de codigo separandolas con comas, pero siempre y cuando sean del mismo tipo
	Definir resultado Como Logico;
	a <- 10; // Se declaran valores a las variables
	b <- 15;
	c <- 20;
	resultado <- No ((a>b) O (b>c)); // Se evalua la expresion en base a la prioridad general de los operadores.
	Escribir resultado;
FinProceso
